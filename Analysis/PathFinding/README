This folder has become to a place storing the codes which finds the true path
based on a series of pressure sensor values. Also make_ele_traj.py makes the
elevation trajectories of each segment in the given .tfix file.
